(function() {
  var DeprecationCopView, viewUri;

  DeprecationCopView = require('./deprecation-cop-view');

  viewUri = 'atom://deprecation-cop';

  module.exports = {
    activate: function() {
      atom.workspace.registerOpener(function(uriToOpen) {
        if (uriToOpen !== viewUri) {
          return;
        }
        return this.deprecationCopView = new DeprecationCopView(uriToOpen);
      });
      return atom.workspaceView.command('deprecation-cop:view', function() {
        return atom.workspaceView.open(viewUri);
      });
    },
    deactivate: function() {
      var _ref;
      return (_ref = this.deprecationCopView) != null ? _ref.destroy() : void 0;
    }
  };

}).call(this);
