(function() {
  var EditorView, SettingEditorView,
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  EditorView = require('atom').EditorView;

  module.exports = SettingEditorView = (function(_super) {
    __extends(SettingEditorView, _super);

    function SettingEditorView(options) {
      if (options == null) {
        options = {};
      }
      options.mini = true;
      SettingEditorView.__super__.constructor.call(this, options);
    }

    SettingEditorView.prototype.setFontSize = function(fontSize) {
      fontSize = parseInt(fontSize) || 0;
      fontSize = Math.min(32, fontSize);
      fontSize = Math.max(10, fontSize);
      return SettingEditorView.__super__.setFontSize.call(this, fontSize);
    };

    return SettingEditorView;

  })(EditorView);

}).call(this);
