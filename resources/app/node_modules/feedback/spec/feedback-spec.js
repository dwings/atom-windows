(function() {
  var $, Q, WorkspaceView, _ref;

  _ref = require('atom'), $ = _ref.$, WorkspaceView = _ref.WorkspaceView;

  Q = require('q');

  describe("Feedback", function() {
    var form;
    form = [][0];
    beforeEach(function() {
      atom.workspaceView = new WorkspaceView;
      return waitsForPromise(function() {
        return atom.packages.activatePackage('feedback');
      });
    });
    return it("displays the feedback view", function() {
      expect(atom.workspaceView.find('.feedback')).not.toExist();
      atom.workspaceView.trigger('feedback:show');
      return expect(atom.workspaceView.find('.feedback')).toExist();
    });
  });

}).call(this);
