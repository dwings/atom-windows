#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir//bin/bash" ]; then
  "$basedir//bin/bash"  "$basedir/../atom-package-manager/bin/apm" "$@"
  ret=$?
else 
  /bin/bash  "$basedir/../atom-package-manager/bin/apm" "$@"
  ret=$?
fi
exit $ret
